一对一和一对多：私聊和发朋友圈
注意：topic必须先启动消费者，再启动生产者，不然发布的是废消息！
     如果有多个订阅者，那么订阅者都会受到消息

    61616 9161
                   topic                                                               queue
工作模式： topic必须先启动消费者，再启动生产者，不然发布的是废消息！       负载均衡模式，如果没有消费者，那么消息也不会被废弃；
           如果有多个订阅者，那么订阅者都会受到消息 。                  有多个消费者，一条消息只能发送给一个消费者，并且要求消费者ack消息。


有无状态：           无状态                                            queue数据默认会在mq服务器上以文件的形式保存，activemq
                                                                    在$AMQ_HOME\data\kr-store\data下面，也可以配置成DB存储。

传递完整性：   没有订阅者，消息会被废弃                                     不会被废弃

处理效率：   消息会按照订阅者数量进行复制，但是要结合不同消息协议            一对一，只看消息协议的性能差异
            自身的性能差异